// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/gitwb-c/crm.saas/backend/internal/ent/costumer"
	"github.com/google/uuid"
)

// Costumer is the model entity for the Costumer schema.
type Costumer struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Phone holds the value of the "phone" field.
	Phone string `json:"phone,omitempty"`
	// Email holds the value of the "email" field.
	Email string `json:"email,omitempty"`
	// CreatedAt holds the value of the "createdAt" field.
	CreatedAt time.Time `json:"createdAt,omitempty"`
	// UpdatedAt holds the value of the "updatedAt" field.
	UpdatedAt time.Time `json:"updatedAt,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CostumerQuery when eager-loading is set.
	Edges        CostumerEdges `json:"edges"`
	selectValues sql.SelectValues
}

// CostumerEdges holds the relations/edges for other nodes in the graph.
type CostumerEdges struct {
	// Deals holds the value of the deals edge.
	Deals []*Deal `json:"deals,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
	// totalCount holds the count of the edges above.
	totalCount [1]map[string]int

	namedDeals map[string][]*Deal
}

// DealsOrErr returns the Deals value or an error if the edge
// was not loaded in eager-loading.
func (e CostumerEdges) DealsOrErr() ([]*Deal, error) {
	if e.loadedTypes[0] {
		return e.Deals, nil
	}
	return nil, &NotLoadedError{edge: "deals"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Costumer) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case costumer.FieldName, costumer.FieldPhone, costumer.FieldEmail:
			values[i] = new(sql.NullString)
		case costumer.FieldCreatedAt, costumer.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case costumer.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Costumer fields.
func (_m *Costumer) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case costumer.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case costumer.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case costumer.FieldPhone:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field phone", values[i])
			} else if value.Valid {
				_m.Phone = value.String
			}
		case costumer.FieldEmail:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field email", values[i])
			} else if value.Valid {
				_m.Email = value.String
			}
		case costumer.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field createdAt", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case costumer.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updatedAt", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Costumer.
// This includes values selected through modifiers, order, etc.
func (_m *Costumer) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryDeals queries the "deals" edge of the Costumer entity.
func (_m *Costumer) QueryDeals() *DealQuery {
	return NewCostumerClient(_m.config).QueryDeals(_m)
}

// Update returns a builder for updating this Costumer.
// Note that you need to call Costumer.Unwrap() before calling this method if this Costumer
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Costumer) Update() *CostumerUpdateOne {
	return NewCostumerClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Costumer entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Costumer) Unwrap() *Costumer {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Costumer is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Costumer) String() string {
	var builder strings.Builder
	builder.WriteString("Costumer(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("phone=")
	builder.WriteString(_m.Phone)
	builder.WriteString(", ")
	builder.WriteString("email=")
	builder.WriteString(_m.Email)
	builder.WriteString(", ")
	builder.WriteString("createdAt=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updatedAt=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// NamedDeals returns the Deals named value or an error if the edge was not
// loaded in eager-loading with this name.
func (_m *Costumer) NamedDeals(name string) ([]*Deal, error) {
	if _m.Edges.namedDeals == nil {
		return nil, &NotLoadedError{edge: name}
	}
	nodes, ok := _m.Edges.namedDeals[name]
	if !ok {
		return nil, &NotLoadedError{edge: name}
	}
	return nodes, nil
}

func (_m *Costumer) appendNamedDeals(name string, edges ...*Deal) {
	if _m.Edges.namedDeals == nil {
		_m.Edges.namedDeals = make(map[string][]*Deal)
	}
	if len(edges) == 0 {
		_m.Edges.namedDeals[name] = []*Deal{}
	} else {
		_m.Edges.namedDeals[name] = append(_m.Edges.namedDeals[name], edges...)
	}
}

// Costumers is a parsable slice of Costumer.
type Costumers []*Costumer
